import 'package:flutter/material.dart';

class OrdersProcessing extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Test no 2"),
    );
  }
}